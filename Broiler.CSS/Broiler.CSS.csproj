<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Broiler.CSS</RootNamespace>
    <ImplicitUsings>disable</ImplicitUsings> <!-- MaiRat: please leave disabled - so everyone can trace back used types (even with notepad) -->
    <Nullable>enable</Nullable>
    <Description>Dependency-free CSS syntax, rule, selector, declaration, value, and serialization kernel for Broiler.</Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Broiler.CSS.Tests" />
  </ItemGroup>

</Project>
